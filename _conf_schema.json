{
    "only_admin": {
        "description": "仅管理员可用本插件命令",
        "type": "bool",
        "hint": "",
        "default": false
    },
    "zhuangtai_show_list": {
        "description": "状态-显示项",
        "hint": "使用“状态”命令的显示项",
        "type": "list",
        "options": [
            "cpu_usage(CPU占用)",
            "memory_usage(内存占用)",
            "disk_usage(磁盘占用)",
            "network_sent(网络发送)",
            "network_recv(网络接收)",
            "process_count(进程数量)",
            "network_connections(连接数量)"
        ],
        "default": [
            "cpu_usage(CPU占用)",
            "memory_usage(内存占用)",
            "disk_usage(磁盘占用)",
            "network_sent(网络发送)",
            "network_recv(网络接收)",
            "process_count(进程数量)",
            "network_connections(连接数量)"
        ]
    }
}